name: first-workflow-testing

on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greetings
        run: |
          echo "hi gagan"
          echo "byeeeeeeeeee"
          echo "see you later"

